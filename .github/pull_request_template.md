## Description

<!-- What does this PR change, and why? Lead with the problem, not the diff. -->

## Related issues

<!-- Link every issue this PR addresses. Use "Closes #123" so merging closes it.
     If the PR does anything the linked issues did NOT ask for, say so explicitly
     under "Notes for reviewers" — silent scope creep is the thing this section
     exists to catch. -->

Closes #

## Type of change

- [ ] Bug fix — non-breaking change that fixes incorrect behaviour
- [ ] New feature — non-breaking change that adds functionality
- [ ] Refactor — no change in behaviour
- [ ] Breaking change — alters existing behaviour or contracts
- [ ] Chore — tooling, CI, dependencies, docs

## How has this been tested?

<!-- This repo has no automated test suite (a deliberate decision). `next build`
     is the only automated check, and it does NOT verify behaviour — it only
     proves the code compiles and prerenders. Anything user-facing must be
     checked by hand, so list what you actually clicked. -->

- [ ] `npx next build` passes with zero errors
- [ ] Manually verified in the running app:
  - [ ] <!-- step -->

## User-visible changes

<!-- Anything a user would notice, including changes you consider improvements.
     "None" is a valid and common answer for a refactor. Attach before/after
     screenshots for UI changes. -->

## Notes for reviewers

<!-- Optional. Anything that would be hard to infer from the diff: a decision
     you made and why, a risk you're accepting, something you deliberately left
     out, or a rabbit hole you want a second opinion on. -->

## Checklist

- [ ] I have self-reviewed the diff
- [ ] The change follows the conventions in `CLAUDE.md`
- [ ] No new console errors or warnings
- [ ] Any scope beyond the linked issues is called out above
